Postgres with Chef Zero
=======================
*and Berkshelf!*

1. Install cookbooks `berks install`
2. Start the virtual machine! `vagrant up`
3. Connect to postgres with `psql -h localhost -U postgres`
